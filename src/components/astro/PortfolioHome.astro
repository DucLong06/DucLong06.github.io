---
/**
 * PortfolioHome.astro — Shared home body for EN (/) and VI (/vi/).
 *
 * Content-first: the full server-rendered site is the baseline (SEO/no-JS/a11y).
 * The 3D knowledge graph lives INSIDE the Hero as a progressive-enhancement
 * backdrop + navigation hub (see Hero.astro → GraphCanvas island). There is no
 * separate "3D mode" — the graph and the HTML coexist on one scrollable page.
 */
import Nav from './Nav.astro';
import Hero from './Hero.astro';
import About from './About.astro';
import Marquee from './Marquee.astro';
import Experience from './Experience.astro';
import Skills from './Skills.astro';
import ProjectsGrid from './ProjectsGrid.astro';
import Papers from './Papers.astro';
import GitHubStats from './GitHubStats.astro';
import Contact from './Contact.astro';
import Footer from './Footer.astro';
import type { Lang } from '../../lib/i18n-helpers';

interface Props {
  lang: Lang;
  currentPath: string;
}
const { lang, currentPath } = Astro.props;
---

<Nav lang={lang} />
<main id="main-content" tabindex="-1">
  <Hero lang={lang} />
  <About lang={lang} />
  <Marquee />
  <Experience lang={lang} />
  <Skills lang={lang} />
  <ProjectsGrid id="work" lang={lang} />
  <Papers lang={lang} />
  <GitHubStats lang={lang} />
  <Contact lang={lang} />
</main>
<Footer lang={lang} currentPath={currentPath} />
